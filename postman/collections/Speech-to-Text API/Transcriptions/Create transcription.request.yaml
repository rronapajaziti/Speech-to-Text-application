$kind: http-request
name: Create transcription
url: "{{baseUrl}}/transcriptions/create/"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "audio": 6,
      "reference_text": "Today I am testing an automatic speech recognition system that converts spoken language into text and evaluates its accuracy using word error rate.",
      "model_name": "base"
    }
order: 3000
